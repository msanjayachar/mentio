import Attend from "@/components/attend";

// Q: What should we be doing in this page?
// STATUS: What is the state of the project as a whole right now?
const page = () => {
  return (
    <div>
      <Attend />
    </div>
  );
};

export default page;
